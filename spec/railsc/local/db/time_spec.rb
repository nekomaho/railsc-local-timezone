RSpec.describe Railsc::Local::Db::Time do
  it "has a version number" do
    expect(Railsc::Local::Db::Time::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
